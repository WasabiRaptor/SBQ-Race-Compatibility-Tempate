[
	// this patch adds the safe hub version of the NPC to the random spawn pool for NPCs on that map
	// this is the pool of NPCs that can spawn as a micro
	{
		"op": "add",
		"path": "/-",
		"value": {
			"npc" : "speciesName", // species
			"npcTypeName" : "sbqVoreVillager",
			"spawnOnce" : false // set this to true if you want to make sure only one instance of the NPC will attempt to be spawned
		}
	}
]
